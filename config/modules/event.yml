event.play:
  module: app.events
  class: PlayEvent

event.grid.left:
  module: app.events.controls
  class: MoveLeftEvent

event.grid.right:
  module: app.events.controls
  class: MoveRightEvent

event.grid.down:
  module: app.events.controls
  class: MoveDownEvent

event.game.over:
  module: app.events
  class: GameOver
